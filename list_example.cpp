#include<bits/stdc++.h>
using namespace std;

int main(){
    list<int>ls;
    ls.push_back(5);
    ls.emplace_back(6);
    ls.emplace_front(4);
    ls.push_front(3);
    //all similar to vector
    
}